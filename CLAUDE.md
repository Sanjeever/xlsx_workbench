# xlsx_workbench

AI 驱动的 Excel 文件分析工作台。用户将 xlsx 文件放入 `data/` 目录，通过对话描述分析需求，由 agent 自动完成分析并将结果写入 `output/`。

## 环境与运行方式

本项目**不预置任何 `.py` 脚本**，所有分析任务通过 uv inline script（PEP 723）完成。代码模板、代码契约与运行命令详见 `.claude/skills/xlsx/SKILL.md`。

- 不要直接调用 `python` 或 `pip`，始终用 `uv run`
- **不得在 `.claude/skills/` 下创建任何 `.py` 文件**

### 多 Claude Code 并发隔离

为避免多个 Claude Code session 同时操作 `output/` 互相覆盖，每个 session 在自己的子目录里工作：

- **首次执行分析前**，用以下命令生成 6 位 hex `<session_id>`（如 `a3f9b2`），整个 Claude Code session 内复用、不要中途换：
  ```bash
  uv run python -c "import secrets; print(secrets.token_hex(3))"
  ```
  然后 `mkdir -p output/s_<session_id>/`
- 所有脚本与产物都写到 `output/s_<session_id>/` 下，例如 `output/s_a3f9b2/_tmp.py`、`output/s_a3f9b2/销售合同表_report.md`
- 跨 session 互不可见、互不删除——只读 / 写 / 删自己子目录内的东西

## 目录约定

| 目录 | 用途 |
|------|------|
| `data/` | 用户放置 xlsx 输入文件的目录（多 session 只读共享） |
| `output/s_<session_id>/` | 当前 session 的所有产物（图表、CSV、报告、`_tmp.py`） |
| `.claude/skills/xlsx/SKILL.md` | 唯一的分析 skill（含代码模板与约定） |

## 分析流程

每次分析按以下步骤推进：

1. **确认目标文件**：用 Glob 列出 `data/*.xlsx`
   - 1 个 → 直接用
   - 多个且未指定 → 用 `AskUserQuestion` 列出文件名让用户选
   - 0 个 → 提示用户放文件后再试，停止
2. **首次接触新文件**且用户意图不明确时，先跑结构探索（SKILL.md 模板 1），向用户报告 sheet 数、行数、列名、数值/类别列、缺失情况
3. **选模板**（见 SKILL.md 代码模板），写入 `output/s_<session_id>/_tmp.py`，只改参数、路径占位符与必要逻辑
4. **运行 + 清理**：按 SKILL.md 运行命令执行
5. **汇报**：列出本次新生成的文件（写完整路径），并给 3–5 条结论性 bullet

## 新主题检测与子目录清理

每轮对话开始前，判断用户是否开启了**新的分析主题**。满足以下任一条件即视为新主题：

- 切换了 xlsx 文件（`data/` 下的目标文件与上一轮不同）
- 分析目标明显转换（如从"探索销售数据"转向"分析用户留存"）
- 用户明确说"重新开始"、"换一个"、"新的分析"

**检测到新主题时**，**只检查自己的 session 子目录**（`output/s_<session_id>/`）——不要枚举 `output/` 下其他 session 的目录。用 Glob 列出当前子目录的现有文件，若非空则询问用户：

> `output/s_<session_id>/` 里有 N 个文件（列出文件名）。开始新分析前要清空吗？

- 用户确认清空 → `rm output/s_<session_id>/*`（`_tmp.py` 同样删除），然后继续分析
- 用户选择保留 → 直接继续，不删除任何文件

**不触发询问的情况**：
- 同一主题的后续步骤（如先探索再画图）
- 自己的 session 子目录为空或尚未创建
- 用户本轮请求本身就是"清空 output"

## 大文件处理

行数 > 50000 时，告知用户数据量较大，先对数值列进行抽样分析（`df.sample(10000, random_state=42)`），完成后说明已抽样及样本量。

## 行为准则

Agent 的核心原则：**先理解，再行动，过程中持续确认**。用户最初给的需求往往不够清晰，不要揣测，不要默认，不要一口气做到底。

**遇到不明确的任务，必须向用户确认任务目标后再动手，严禁猜测用户意图。** 确认时优先使用 `AskUserQuestion` 工具，将需要澄清的点整理成结构化选项，让用户一键选择，而非要求用户打一大段字回复。

### 任务启动前

接手任何分析任务时，先向用户确认以下关键信息，而不是直接动手。每个确认点都用 `AskUserQuestion` 以结构化选项呈现：

1. **目标文件** — `data/` 下可能有多个 xlsx，用户没说清楚时，用 `AskUserQuestion` 列出文件让用户选：
   - header: "数据文件"，question: "要分析哪个文件？"，options: 每个文件一个选项
2. **分析目标** — 用户说"帮我分析这张表"时，用 `AskUserQuestion` 追问：
   - header: "分析方向"，question: "你最关心什么？"，options: 如"看数据概览与缺失情况"、"深入某个维度的统计"、"找异常值"、"出可视化报告"等，也可设 `multiSelect: true` 允许多选
3. **范围与边界** — 目标明确后，视情况继续用 `AskUserQuestion` 确认范围：
   - header: "分析范围"，question: "范围有什么限制？"，options: 如"全部数据"、"只看最近一年"、"排除金额为 0 的记录"等

**禁止行为**：
- 看到 xlsx 直接 `df.describe()` 一把梭，然后生成一堆图
- 用户说"探索一下"就自己决定方向
- 不确认分析目标就开始写脚本
- 用纯文本让用户手打一大段回复来代替 AskUserQuestion

### 执行过程中

1. **分步推进，每步做一次确认** — 先做基础探索（结构、缺失、类型），把发现反馈给用户，**等用户给方向**后再做深入分析
2. **中间结果先展示** — 生成图表或统计结果后，先描述关键发现，再问用户下一步想怎么走
3. **遇到歧义立即停** — 对数据含义、口径、过滤条件有任何不确定，停下来问，不要猜

**禁止行为**：
- 一次性生成 5 张图一份报告，用户说"我只想看看缺失率"
- 发现数据问题（如异常值、空值）自行决定处理方式

### 任务收尾

1. 对照用户的**原始需求**（不是自己中途理解的需求），逐条确认产出是否覆盖了
2. 如果用户对某些输出不满意，优先修改而非追加新输出

## 输出汇总与结论解读

每次分析结束后**必须**两件事都做：

**1. 列出本次生成的所有产物（带 session 子目录的完整路径）**

```
output/s_<session_id>/文件名.csv  — 一句话说明内容
output/s_<session_id>/文件名.png  — 一句话说明内容
```

**2. 给出 3–5 条结论性 bullet**

不是"已生成 X.png"，而是基于数据的**发现**：

- ✅ "购买合同占 62%（412/668），是主导类型"
- ✅ "产品 3 销量最高（共 156 件），但单笔金额低于均值"
- ✅ "总金额与购买数量相关系数 0.78，正强相关"
- ❌ "已生成柱状图"（这只是过程描述，不是发现）

数据不支持给出有价值结论时，直接说明"数据未呈现明显规律"，不要硬凑。

